---
phase: 06-hotspot-customization-options-icons-info-panels-video-support-and-adaptive-screen-layout
plan: 03
type: execute
wave: 2
depends_on:
  - "06-02"
files_modified:
  - src/components/viewer/HotspotMarker.tsx
autonomous: true
requirements:
  - HS6-03

must_haves:
  truths:
    - "Clicking a navigation hotspot still calls the onClick prop for scene transition — behavior is unchanged"
    - "Clicking an info, media, or link hotspot calls setActiveHotspot(hotspot._id) from useViewerStore"
    - "The 240px inline popup card is removed entirely from HotspotMarker"
    - "When iconName is set on a hotspot, the corresponding ICON_REGISTRY Lucide icon renders instead of the type-default"
    - "When iconName is not set or not found in registry, the type-based default icon renders as before"
    - "When accentColor is set, the marker button uses that color instead of the type-based color"
  artifacts:
    - path: "src/components/viewer/HotspotMarker.tsx"
      provides: "Refactored hotspot marker — renders button/tooltip only, delegates panel open to Zustand"
      contains: "useViewerStore"
  key_links:
    - from: "src/components/viewer/HotspotMarker.tsx"
      to: "src/hooks/useViewerStore.ts"
      via: "setActiveHotspot(hotspot._id) called on non-navigation hotspot click"
      pattern: "setActiveHotspot\\(hotspot\\._id\\)"
---

<objective>
Refactor HotspotMarker.tsx to: remove the inline 240px popup card, delegate panel-open to the Zustand useViewerStore, add ICON_REGISTRY for custom icon rendering, and support accentColor override on the marker button.

Purpose: The existing inline popup card rendered inside the R3F Html component cannot exceed ~240px without z-index conflicts. Delegating panel state to Zustand allows the richer HotspotInfoPanel (rendered outside the Canvas in Plan 04/05) to respond to clicks without any Canvas boundary issues.

Output: Modified src/components/viewer/HotspotMarker.tsx. The component becomes focused: Html wrapper, marker button with animations and tooltip, and click handler that delegates to store.
</objective>

<execution_context>
@/Users/padidamabhinay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/padidamabhinay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-hotspot-customization-options-icons-info-panels-video-support-and-adaptive-screen-layout/06-RESEARCH.md
@src/components/viewer/HotspotMarker.tsx
@src/hooks/useViewerStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor HotspotMarker — ICON_REGISTRY, store delegation, remove popup card</name>
  <files>src/components/viewer/HotspotMarker.tsx</files>
  <action>
    Read src/components/viewer/HotspotMarker.tsx in full before editing. Then apply these changes:

    **1. Update imports:**
    - Keep: `useState` from react (still needed for `isHovered` tooltip state)
    - Keep: `Html` from '@react-three/drei'
    - Keep existing Lucide icons: `Navigation, Info, Play, ExternalLink, ChevronRight, X`
    - Remove: `ChevronRight` and `X` can be removed — they were only used in the popup card
    - Add: `Home, Bed, Bath, Car, Wifi, Camera, Star, DollarSign, Ruler, Trees, Sun, Building2, Key` from 'lucide-react'
    - Add: `import { useViewerStore } from '@/hooks/useViewerStore'`

    **2. Add ICON_REGISTRY constant after TYPE_CONFIG:**
    ```typescript
    const ICON_REGISTRY: Record<string, React.ComponentType<{ size?: number; strokeWidth?: number }>> = {
      navigation: Navigation,
      info: Info,
      play: Play,
      link: ExternalLink,
      home: Home,
      bed: Bed,
      bath: Bath,
      car: Car,
      wifi: Wifi,
      camera: Camera,
      star: Star,
      price: DollarSign,
      area: Ruler,
      garden: Trees,
      balcony: Sun,
      building: Building2,
      key: Key,
    }
    ```

    **3. Extend HotspotData interface** with Phase 6 fields (all optional):
    ```typescript
    iconName?: string
    panelLayout?: 'compact' | 'rich' | 'video'
    videoUrl?: string
    ctaLabel?: string
    ctaUrl?: string
    accentColor?: string
    ```

    **4. In the component function body:**
    - Keep: `const [isHovered, setIsHovered] = useState(false)` — tooltip still uses it
    - Remove: `const [isPopupOpen, setIsPopupOpen] = useState(false)` — inline popup is gone
    - Add: `const setActiveHotspot = useViewerStore((s) => s.setActiveHotspot)`
    - Add: `const IconComponent = (hotspot.iconName ? ICON_REGISTRY[hotspot.iconName] : undefined) ?? config.icon`
    - Add: `const markerColor = hotspot.accentColor ?? config.color`

    **5. Navigation hotspot branch — NO CHANGES.** The navigation type block (atan2 arrow, Gold pulse ring, onClick prop call) must remain exactly as-is. It does not use the store.

    **6. For the info/media/link marker button (the non-navigation button):**
    - Change the onClick handler from `onClick={() => setIsPopupOpen((prev) => !prev)}` to `onClick={() => setActiveHotspot(hotspot._id)}`
    - Replace `config.color` in the marker button backgroundColor with `markerColor`
    - Replace `config.icon` rendering with `IconComponent` (already computed above)

    **7. Remove the entire popup card block** — delete everything from the comment `{/* Popup card */}` through the closing `)}` of the `{isPopupOpen && (...)}` block. This is approximately lines 214–356 in the current file. The info panel is now rendered outside the Canvas.

    **8. Keep unchanged:** tooltip label on hover, `isSelected` box shadow styling, `visible === false` early return, Html zIndexRange, onMouseEnter/onMouseLeave handlers.

    CRITICAL: Navigation type must call `onClick` prop (scene transition). Info/media/link types must call `setActiveHotspot(hotspot._id)`. These two code paths must NEVER merge.
  </action>
  <verify>
    - npm run lint passes with no new errors
    - `isPopupOpen` state is gone from the file
    - ICON_REGISTRY is defined with ~17 entries
    - `useViewerStore` is imported and `setActiveHotspot` is called on info/media/link click
    - Navigation hotspot still calls `onClick` prop
    - No popup card JSX remains in the file
  </verify>
  <done>HotspotMarker.tsx renders the marker button and tooltip only. Popup card is removed. Custom icon renders via ICON_REGISTRY when iconName is set. Non-navigation clicks delegate to useViewerStore. Navigation behavior unchanged.</done>
</task>

</tasks>

<verification>
1. npm run lint passes
2. No `isPopupOpen` or `setIsPopupOpen` anywhere in the file
3. ICON_REGISTRY has entries for: navigation, info, play, link, home, bed, bath, car, wifi, camera, star, price, area, garden, balcony, building, key
4. Navigation hotspot onClick calls the prop function (scene transition)
5. Info/media/link hotspot onClick calls setActiveHotspot(hotspot._id)
6. accentColor override is applied to marker button color
</verification>

<success_criteria>
HotspotMarker.tsx is a focused marker-button component. The inline popup card is gone. Panel state is in Zustand. Custom Lucide icons render from ICON_REGISTRY. Navigation hotspot behavior is completely unchanged from Phase 1.
</success_criteria>

<output>
After completion, create `.planning/phases/06-hotspot-customization-options-icons-info-panels-video-support-and-adaptive-screen-layout/06-03-SUMMARY.md`
</output>
