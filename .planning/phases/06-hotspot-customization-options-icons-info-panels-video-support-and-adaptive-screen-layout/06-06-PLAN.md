---
phase: 06-hotspot-customization-options-icons-info-panels-video-support-and-adaptive-screen-layout
plan: 06
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - src/app/(dashboard)/tours/[id]/edit/page.tsx
autonomous: true
requirements:
  - HS6-06

must_haves:
  truths:
    - "The hotspot creation form shows an icon picker grid of 17 icons for non-navigation types"
    - "Selecting an icon in the grid highlights it and 'None' deselects"
    - "The form has a Panel Layout dropdown (Compact / Rich / Video) for non-navigation types"
    - "The form has CTA Label and CTA URL inputs for info and link types"
    - "Creating a hotspot with these fields set passes iconName, panelLayout, ctaLabel, ctaUrl to the createHotspot mutation"
    - "Form resets all new fields after successful hotspot creation"
  artifacts:
    - path: "src/app/(dashboard)/tours/[id]/edit/page.tsx"
      provides: "Tour editor hotspot form extended with Phase 6 customization fields"
      contains: "hotspotIconName"
  key_links:
    - from: "src/app/(dashboard)/tours/[id]/edit/page.tsx"
      to: "convex/hotspots.ts"
      via: "handleConfirmHotspot calls createHotspot mutation with new optional fields"
      pattern: "createHotspot.*iconName"
---

<objective>
Extend the tour editor hotspot creation form with four new field groups: icon picker grid (17 Lucide icons), panel layout selector (Compact/Rich/Video), CTA Label/URL inputs (info and link types), and wire them into the createHotspot mutation call.

Purpose: Without editor support, the Phase 6 schema fields (iconName, panelLayout, ctaLabel, ctaUrl) are never populated in practice. Agents need a way to configure per-hotspot customization when adding hotspots in the tour editor.

Output: Modified src/app/(dashboard)/tours/[id]/edit/page.tsx. Only the hotspot creation form section is changed. The editor's activePopupHotspot state (separate local state for inline preview) is untouched.
</objective>

<execution_context>
@/Users/padidamabhinay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/padidamabhinay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-hotspot-customization-options-icons-info-panels-video-support-and-adaptive-screen-layout/06-RESEARCH.md
@src/app/(dashboard)/tours/[id]/edit/page.tsx
@convex/hotspots.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add state variables, icon registry constant, and form reset updates</name>
  <files>src/app/(dashboard)/tours/[id]/edit/page.tsx</files>
  <action>
    Read src/app/(dashboard)/tours/[id]/edit/page.tsx carefully before editing. Focus on:
    - Line 52: `const HOTSPOT_TYPES = [...]` (existing constant — keep unchanged)
    - Lines 94-106: hotspot creation state variables
    - Lines 432-455: `handleConfirmHotspot` — the createHotspot call and reset block
    - The lucide-react import block (lines 12-33): already imports Navigation, Info, Play, ExternalLink

    **A. Add new Lucide icon imports** alongside existing ones in the lucide-react import block:
    ```typescript
    Home, Bed, Bath, Car, Wifi, Camera, Star, DollarSign, Ruler, Trees, Sun, Building2, Key
    ```
    These are added to the existing `import { ..., Navigation, Info, Play, ExternalLink, ... } from 'lucide-react'` line.

    **B. Add EDITOR_ICON_OPTIONS constant** after the existing `HOTSPOT_TYPES` constant at line 52:
    ```typescript
    const EDITOR_ICON_OPTIONS = [
      { key: 'navigation', label: 'Navigate', icon: Navigation },
      { key: 'info', label: 'Info', icon: Info },
      { key: 'play', label: 'Play', icon: Play },
      { key: 'link', label: 'Link', icon: ExternalLink },
      { key: 'home', label: 'Home', icon: Home },
      { key: 'bed', label: 'Bedroom', icon: Bed },
      { key: 'bath', label: 'Bathroom', icon: Bath },
      { key: 'car', label: 'Parking', icon: Car },
      { key: 'wifi', label: 'WiFi', icon: Wifi },
      { key: 'camera', label: 'Photo', icon: Camera },
      { key: 'star', label: 'Feature', icon: Star },
      { key: 'price', label: 'Price', icon: DollarSign },
      { key: 'area', label: 'Area', icon: Ruler },
      { key: 'garden', label: 'Garden', icon: Trees },
      { key: 'balcony', label: 'Balcony', icon: Sun },
      { key: 'building', label: 'Building', icon: Building2 },
      { key: 'key', label: 'Key', icon: Key },
    ] as const
    ```

    **C. Add 4 new useState variables** in the hotspot creation state section (after `const [hotspotVisible, setHotspotVisible] = useState(true)` at line 106):
    ```typescript
    const [hotspotIconName, setHotspotIconName] = useState<string>('')
    const [hotspotPanelLayout, setHotspotPanelLayout] = useState<'compact' | 'rich' | 'video'>('compact')
    const [hotspotCtaLabel, setHotspotCtaLabel] = useState('')
    const [hotspotCtaUrl, setHotspotCtaUrl] = useState('')
    ```

    Note: Do NOT add a separate `hotspotVideoUrl` state — the existing `hotspotContent` state already stores the media URL for media-type hotspots and maps to the `content` field. The dedicated `videoUrl` schema field is for future use; omit it from the editor form to keep scope focused.

    **D. Reset new fields in handleConfirmHotspot** — find the reset block after `setHotspotVisible(true)` at line 455. Add:
    ```typescript
    setHotspotIconName('')
    setHotspotPanelLayout('compact')
    setHotspotCtaLabel('')
    setHotspotCtaUrl('')
    ```

    **E. Pass new fields in the createHotspot call** at line 432. After `visible: hotspotVisible,` add:
    ```typescript
    iconName: hotspotIconName || undefined,
    panelLayout: hotspotPanelLayout,
    ctaLabel: hotspotCtaLabel || undefined,
    ctaUrl: hotspotCtaUrl || undefined,
    ```

    **F. Add new deps to handleConfirmHotspot useCallback** deps array at line 461. Add after `hotspotVisible,`:
    ```typescript
    hotspotIconName,
    hotspotPanelLayout,
    hotspotCtaLabel,
    hotspotCtaUrl,
    ```
  </action>
  <verify>npm run lint passes. New state variables exist. EDITOR_ICON_OPTIONS constant has 17 entries. createHotspot call includes iconName, panelLayout, ctaLabel, ctaUrl. Reset block resets all 4 new fields.</verify>
  <done>New state, EDITOR_ICON_OPTIONS constant, createHotspot args, and reset logic all updated. No existing logic changed.</done>
</task>

<task type="auto">
  <name>Task 2: Add icon picker, panel layout selector, and CTA fields to the hotspot form JSX</name>
  <files>src/app/(dashboard)/tours/[id]/edit/page.tsx</files>
  <action>
    Read the current state of src/app/(dashboard)/tours/[id]/edit/page.tsx (including Task 1 changes). Find the hotspot form JSX starting around line 1063. The form structure is:
    Type selector → Title → Label → Visibility toggle → (type-specific fields: navigation/info/link/media/image).

    Add the following new sections AFTER the image upload section (after the `(hotspotType === 'info' || hotspotType === 'media')` image block, around line 1229) and BEFORE the Add/Cancel buttons.

    **A. Icon picker — show for non-navigation types:**
    ```tsx
    {hotspotType !== 'navigation' && (
      <div className="flex flex-col gap-1.5">
        <label className="text-[11px] font-semibold uppercase tracking-wider" style={{ color: '#6B6560' }}>
          Icon (optional)
        </label>
        <div style={{ display: 'flex', flexWrap: 'wrap', gap: 5 }}>
          {/* "None" option — resets to type-default icon */}
          <button
            type="button"
            onClick={() => setHotspotIconName('')}
            title="Default (type icon)"
            style={{
              width: 34, height: 34, borderRadius: 6,
              border: `1px solid ${hotspotIconName === '' ? '#D4A017' : 'rgba(255,255,255,0.08)'}`,
              backgroundColor: hotspotIconName === '' ? 'rgba(212,160,23,0.1)' : '#0A0908',
              display: 'flex', alignItems: 'center', justifyContent: 'center',
              cursor: 'pointer', color: '#6B6560', fontSize: 11,
            }}
          >
            —
          </button>
          {EDITOR_ICON_OPTIONS.map(({ key, label, icon: IconComp }) => (
            <button
              type="button"
              key={key}
              onClick={() => setHotspotIconName(key)}
              title={label}
              aria-label={label}
              style={{
                width: 34, height: 34, borderRadius: 6,
                border: `1px solid ${hotspotIconName === key ? '#D4A017' : 'rgba(255,255,255,0.08)'}`,
                backgroundColor: hotspotIconName === key ? 'rgba(212,160,23,0.1)' : '#0A0908',
                display: 'flex', alignItems: 'center', justifyContent: 'center',
                cursor: 'pointer',
                color: hotspotIconName === key ? '#D4A017' : '#6B6560',
              }}
            >
              <IconComp size={15} strokeWidth={1.5} />
            </button>
          ))}
        </div>
      </div>
    )}
    ```

    **B. Panel Layout selector — show for non-navigation types:**
    ```tsx
    {hotspotType !== 'navigation' && (
      <div className="flex flex-col gap-1.5">
        <label className="text-[11px] font-semibold uppercase tracking-wider" style={{ color: '#6B6560' }}>
          Panel Layout
        </label>
        <select
          value={hotspotPanelLayout}
          onChange={(e) => setHotspotPanelLayout(e.target.value as 'compact' | 'rich' | 'video')}
          className="w-full h-9 px-3 rounded-lg text-[12px] outline-none"
          style={{ backgroundColor: '#0A0908', border: '1px solid rgba(212,160,23,0.15)', color: '#F5F3EF', fontFamily: 'var(--font-dmsans)' }}
        >
          <option value="compact">Compact</option>
          <option value="rich">Rich (image + CTA)</option>
          <option value="video">Video modal</option>
        </select>
      </div>
    )}
    ```

    **C. CTA Label and URL fields — show for info and link types:**
    ```tsx
    {(hotspotType === 'info' || hotspotType === 'link') && (
      <>
        <div className="flex flex-col gap-1.5">
          <label className="text-[11px] font-semibold uppercase tracking-wider" style={{ color: '#6B6560' }}>
            CTA Button Label
          </label>
          <input
            type="text"
            value={hotspotCtaLabel}
            onChange={(e) => setHotspotCtaLabel(e.target.value)}
            placeholder="e.g. Schedule a Viewing"
            maxLength={40}
            className="w-full h-9 px-3 rounded-lg text-[12px] outline-none"
            style={{ backgroundColor: '#0A0908', border: '1px solid rgba(212,160,23,0.15)', color: '#F5F3EF', fontFamily: 'var(--font-dmsans)' }}
            onFocus={(e) => { e.currentTarget.style.borderColor = '#D4A017' }}
            onBlur={(e) => { e.currentTarget.style.borderColor = 'rgba(212,160,23,0.15)' }}
          />
        </div>
        {hotspotCtaLabel.trim() && (
          <div className="flex flex-col gap-1.5">
            <label className="text-[11px] font-semibold uppercase tracking-wider" style={{ color: '#6B6560' }}>
              CTA Button URL
            </label>
            <input
              type="url"
              value={hotspotCtaUrl}
              onChange={(e) => setHotspotCtaUrl(e.target.value)}
              placeholder="https://..."
              className="w-full h-9 px-3 rounded-lg text-[12px] outline-none"
              style={{ backgroundColor: '#0A0908', border: '1px solid rgba(212,160,23,0.15)', color: '#F5F3EF', fontFamily: 'var(--font-dmsans)' }}
              onFocus={(e) => { e.currentTarget.style.borderColor = '#D4A017' }}
              onBlur={(e) => { e.currentTarget.style.borderColor = 'rgba(212,160,23,0.15)' }}
            />
          </div>
        )}
      </>
    )}
    ```

    Placement: Insert all three blocks (A, B, C) immediately before the Add Hotspot / Cancel buttons at the bottom of the scrollable form body. Find the button block that calls `handleConfirmHotspot` and place the new fields just above it.
  </action>
  <verify>
    - npm run lint passes
    - Open tour editor, place a hotspot: icon picker grid appears for non-navigation types
    - Selecting an icon highlights its border in gold (#D4A017)
    - Panel layout dropdown shows three options
    - CTA label/URL inputs appear when type is info or link
    - Creating a hotspot passes iconName and panelLayout to Convex
  </verify>
  <done>Hotspot form shows icon picker (17 icons + None), panel layout selector (Compact/Rich/Video), and CTA Label/URL fields (info/link types only). All fields are passed to createHotspot and reset after successful creation.</done>
</task>

</tasks>

<verification>
1. npm run lint passes
2. EDITOR_ICON_OPTIONS has 17 entries — each renders as a 34x34 button in the icon grid
3. Panel layout dropdown has exactly: Compact, Rich (image + CTA), Video modal
4. CTA Label shows for info and link types; CTA URL shows only when CTA Label is non-empty
5. createHotspot call includes iconName, panelLayout, ctaLabel, ctaUrl
6. All 4 new fields are reset in the post-submit reset block
7. Navigation type: icon picker and panel layout do NOT show
</verification>

<success_criteria>
Agents can configure Phase 6 hotspot customization in the tour editor: select a custom Lucide icon from a 17-icon grid, choose a panel layout variant, and optionally add a CTA button label and URL for info/link hotspots. All fields are saved to Convex on hotspot creation.
</success_criteria>

<output>
After completion, create `.planning/phases/06-hotspot-customization-options-icons-info-panels-video-support-and-adaptive-screen-layout/06-06-SUMMARY.md`
</output>
