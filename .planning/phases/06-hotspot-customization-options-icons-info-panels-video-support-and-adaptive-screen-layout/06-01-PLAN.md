---
phase: 06-hotspot-customization-options-icons-info-panels-video-support-and-adaptive-screen-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/schema.ts
  - convex/hotspots.ts
autonomous: true
requirements:
  - HS6-01

must_haves:
  truths:
    - "Existing hotspots load without schema validation errors after the push"
    - "New hotspot documents can be created with iconName, panelLayout, videoUrl, ctaLabel, ctaUrl, and accentColor fields"
    - "Mutations accept the six new optional fields without breaking existing call sites that omit them"
  artifacts:
    - path: "convex/schema.ts"
      provides: "hotspots table with 6 new optional fields"
      contains: "iconName"
    - path: "convex/hotspots.ts"
      provides: "create and update mutations accepting new fields"
      contains: "panelLayout"
  key_links:
    - from: "convex/schema.ts"
      to: "convex/hotspots.ts"
      via: "mutation args must match schema validator types"
      pattern: "v\\.optional.*panelLayout"
---

<objective>
Extend the Convex hotspots schema and mutations with six new optional customization fields: iconName, panelLayout, videoUrl, ctaLabel, ctaUrl, and accentColor.

Purpose: Frontend components in later plans (HotspotMarker icon display, HotspotInfoPanel layout variant, video modal, CTA button, per-hotspot accent color) all read these fields from Convex documents. They must exist in the schema before any frontend work proceeds.

Output: Updated `convex/schema.ts` and `convex/hotspots.ts`. No UI changes in this plan.
</objective>

<execution_context>
@/Users/padidamabhinay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/padidamabhinay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-hotspot-customization-options-icons-info-panels-video-support-and-adaptive-screen-layout/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend hotspots schema with 6 new optional fields</name>
  <files>convex/schema.ts</files>
  <action>
    In `convex/schema.ts`, locate the `hotspots` defineTable block (currently ends at line 169 with `.index('by_sceneId', ['sceneId'])`).

    Add the following six fields AFTER the existing `visible: v.optional(v.boolean()),` line and BEFORE the closing `}).index(...)`:

    ```
    // Phase 6 additions — all optional for backward compatibility
    iconName: v.optional(v.string()),
    panelLayout: v.optional(v.union(v.literal('compact'), v.literal('rich'), v.literal('video'))),
    videoUrl: v.optional(v.string()),
    ctaLabel: v.optional(v.string()),
    ctaUrl: v.optional(v.string()),
    accentColor: v.optional(v.string()),
    ```

    CRITICAL: All six fields MUST use `v.optional()`. Do not add any required fields — Convex will reject existing documents without them on `convex dev`. Do not modify any existing fields.
  </action>
  <verify>Run `npx convex dev` (or check terminal if already running) — it should push the schema without validation errors. No existing hotspot documents should fail.</verify>
  <done>Schema push succeeds with no errors. `convex/schema.ts` hotspots table has 6 new optional fields after `visible`.</done>
</task>

<task type="auto">
  <name>Task 2: Extend create and update mutations to accept new fields</name>
  <files>convex/hotspots.ts</files>
  <action>
    In `convex/hotspots.ts`, add the six new optional fields to both the `create` and `update` mutation args objects.

    For `create` mutation args, add after `visible: v.optional(v.boolean()),`:
    ```
    iconName: v.optional(v.string()),
    panelLayout: v.optional(v.union(v.literal('compact'), v.literal('rich'), v.literal('video'))),
    videoUrl: v.optional(v.string()),
    ctaLabel: v.optional(v.string()),
    ctaUrl: v.optional(v.string()),
    accentColor: v.optional(v.string()),
    ```

    For `update` mutation args (which already uses v.optional on most fields), add the same six fields after `visible: v.optional(v.boolean()),`.

    The `create` handler (`ctx.db.insert('hotspots', args)`) already spreads all args, so it will automatically persist the new fields. The `update` handler already uses `cleanUpdates` pattern (filters out undefined values), so no handler logic changes are needed.

    Do NOT change the `listByScene`, `listByTour`, or `remove` functions.
  </action>
  <verify>TypeScript compilation: `npm run lint` should pass without new errors. The mutation args in hotspots.ts match the schema validators (same optional types).</verify>
  <done>Both `create` and `update` mutations in `convex/hotspots.ts` accept the 6 new optional fields. Existing call sites that omit the new fields continue to work unchanged.</done>
</task>

</tasks>

<verification>
1. `npx convex dev` accepts the schema push with no validation errors
2. `npm run lint` passes without new type errors in convex/hotspots.ts
3. All 6 new fields use `v.optional()` in both schema.ts and hotspots.ts
4. No existing fields modified in either file
</verification>

<success_criteria>
The hotspots table in Convex supports storing iconName (string), panelLayout (compact | rich | video), videoUrl (string), ctaLabel (string), ctaUrl (string), and accentColor (string) — all optional for backward compatibility with existing hotspot documents.
</success_criteria>

<output>
After completion, create `.planning/phases/06-hotspot-customization-options-icons-info-panels-video-support-and-adaptive-screen-layout/06-01-SUMMARY.md`
</output>
