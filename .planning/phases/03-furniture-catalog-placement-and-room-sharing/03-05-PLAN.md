---
phase: 03-furniture-catalog-placement-and-room-sharing
plan: 05
type: execute
wave: 4
depends_on: [03-04]
files_modified:
  - src/app/tour/[slug]/furnished/[id]/page.tsx
  - src/components/furnished/FurnishedRoomViewer.tsx
  - src/components/furnished/CostSummaryPanel.tsx
  - src/components/viewer/GaussianSplatViewer.tsx
autonomous: true
requirements: [SHARE-01, SHARE-02, SHARE-03]

must_haves:
  truths:
    - "User can save the furnished room and gets a shareable link"
    - "Anyone with the share link sees the furnished room in read-only mode without logging in"
    - "Share page shows an itemized cost summary panel with Amazon product links"
    - "Share page has a toggle to show/hide all furniture (compare empty vs furnished)"
  artifacts:
    - path: "src/app/tour/[slug]/furnished/[id]/page.tsx"
      provides: "Public share page route for furnished rooms"
      min_lines: 30
    - path: "src/components/furnished/FurnishedRoomViewer.tsx"
      provides: "Read-only 3D viewer rendering placed furniture with orbit/zoom"
      min_lines: 60
    - path: "src/components/furnished/CostSummaryPanel.tsx"
      provides: "Itemized cost list with Amazon links for share page"
      min_lines: 50
  key_links:
    - from: "src/app/tour/[slug]/furnished/[id]/page.tsx"
      to: "convex/furnishedRooms.ts"
      via: "useQuery(api.furnishedRooms.getBySlug)"
      pattern: "useQuery.*furnishedRooms"
    - from: "src/components/furnished/FurnishedRoomViewer.tsx"
      to: "src/components/viewer/FurniturePiece.tsx"
      via: "Reuses FurniturePiece in read-only mode (no TransformControls)"
      pattern: "FurniturePiece"
    - from: "src/components/furnished/CostSummaryPanel.tsx"
      to: "placedFurniture data"
      via: "Maps placed items to cost rows with Amazon links"
      pattern: "amazonUrl"
---

<objective>
Build the save and share workflow: save button in the editor that persists furniture placements to Convex, a public share page at `/tour/[slug]/furnished/[id]` that loads the furnished room without authentication, and a cost summary panel with Amazon product links. Includes furniture toggle (show/hide) for comparing empty vs furnished room.

Purpose: The share link is the end product of the furniture feature. Real estate agents save their staging and share it with buyers who can see the furnished room with cost details and buy links.
Output: Working save/share flow and public share page with cost summary.
</objective>

<execution_context>
@/Users/padidamabhinay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/padidamabhinay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-furniture-catalog-placement-and-room-sharing/03-CONTEXT.md
@.planning/phases/03-furniture-catalog-placement-and-room-sharing/03-RESEARCH.md
@.planning/phases/03-furniture-catalog-placement-and-room-sharing/03-04-SUMMARY.md

<interfaces>
<!-- Convex furnished room queries from Plan 01 -->
From convex/furnishedRooms.ts:
```typescript
export const create = mutation({ args: { tourId, title }, ... })
export const getBySlug = query({ args: { tourSlug, furnishedRoomSlug }, ... })
  // Returns: { room, placements: [{ ...placement, item: furnitureItem }], tour }
export const savePlacements = mutation({ args: { furnishedRoomId, placements: [...] }, ... })
```

<!-- FurniturePiece from Plan 03 -->
From src/components/viewer/FurniturePiece.tsx:
```typescript
// Can be used in read-only mode by passing selected=false and no onSelect/onTransformEnd
```

<!-- GaussianSplatViewer from Plan 04 -->
From src/components/viewer/GaussianSplatViewer.tsx:
```typescript
interface GaussianSplatViewerProps {
  splatUrl: string
  tourSlug?: string
  hotspots?: Hotspot3D[]
  enableFurniture?: boolean  // Added in Plan 04
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add save/share flow to GaussianSplatViewer editor</name>
  <files>src/components/viewer/GaussianSplatViewer.tsx</files>
  <action>
Extend GaussianSplatViewer.tsx to add save and share functionality when `enableFurniture` is true:

1. **Save button:** Add a "Save Arrangement" button in the viewer header bar (alongside existing fullscreen/share buttons). Only visible when mode is 'furnish' and placedItems.length > 0.
   - On click: open a dialog (use Radix Dialog from existing ui/ components) asking for an arrangement title (default: "My Arrangement"). On confirm:
     - If no furnished room exists for this session, call `create` mutation with tourId and title to create one.
     - Call `savePlacements` mutation with the furnishedRoomId and current placedItems mapped to the Convex format (furnitureItemId, instanceId, position as {x,y,z}, rotation as {x,y,z}, scale as {x,y,z}).
     - Show toast.success("Arrangement saved! Share link copied.").
     - Copy the share URL `/tour/${tourSlug}/furnished/${roomSlug}` to clipboard.
   - Track the current `furnishedRoomId` in local state (useState) so subsequent saves update the same room.

2. **Share button:** When a furnished room has been saved (furnishedRoomId exists), add a "Share Furnished Room" button that copies the share URL to clipboard. Uses Gold button variant.

3. **Load saved arrangements:** Add `furnishedRoomId?: string` prop to GaussianSplatViewerProps. When provided (editor loading a saved arrangement), query the room's placements on mount and call `useFurnitureStore.getState().loadFromSaved(items)`.

Add `tourId?: string` prop to GaussianSplatViewerProps for the save mutation.
  </action>
  <verify>
    <automated>cd /Users/padidamabhinay/Desktop/UI/Spazeo && npx tsc --noEmit src/components/viewer/GaussianSplatViewer.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Save button persists furniture placements to Convex via savePlacements mutation. Share URL is generated and copied to clipboard. Loading a saved arrangement populates the store from Convex data.</done>
</task>

<task type="auto">
  <name>Task 2: Create public share page and CostSummaryPanel</name>
  <files>src/app/tour/[slug]/furnished/[id]/page.tsx, src/components/furnished/FurnishedRoomViewer.tsx, src/components/furnished/CostSummaryPanel.tsx</files>
  <action>
Create src/app/tour/[slug]/furnished/[id]/page.tsx:
- Next.js page component (server component wrapper with client children).
- Extracts `slug` and `id` from params.
- Renders FurnishedRoomViewer (dynamic import, ssr: false) passing slug and furnishedRoomSlug.
- Page metadata: title from tour name + arrangement title, description for SEO.

Create src/components/furnished/FurnishedRoomViewer.tsx ('use client'):
- Accepts `tourSlug: string` and `furnishedRoomSlug: string`.
- Uses `useQuery(api.furnishedRooms.getBySlug, { tourSlug, furnishedRoomSlug })` to load room data. This is a public query (no auth). Returns room, placements with item metadata, and tour info.
- Uses `useQuery(api.tours.getBySlug, { slug: tourSlug })` to get the splatUrl.
- **Layout:** Flex container. Left/main: 3D viewer canvas. Right sidebar (desktop) / bottom sheet (mobile): CostSummaryPanel.
- **3D viewer:** Reuse GaussianSplatViewer with `enableFurniture={false}` (read-only). OR render a simpler read-only Canvas:
  - R3F Canvas with OrbitControls (orbit + zoom only, no click-to-move needed)
  - SplatScene for the Gaussian Splat
  - Map placements to `<FurniturePiece>` components with `selected={false}` (no TransformControls, no selection)
- **Furniture toggle:** A button (Eye/EyeOff icon) to show/hide all furniture items. Toggling sets a local state that controls whether FurniturePiece components are rendered. This lets viewers compare empty vs furnished room.
- **Tour branding:** If tour has brandingConfig (logo, color), show it in the viewer header.
- Show loading state while query resolves. Show 404 message if room not found.

Create src/components/furnished/CostSummaryPanel.tsx ('use client'):
- Accepts `placements` array (each with item metadata: name, price, amazonUrl) and `furnitureVisible: boolean`.
- **Header:** "Furniture Cost Summary" with total cost and item count.
- **Itemized list:** Scrollable list of items. Each row: item name, formatted price (USD), and ExternalLink icon linking to amazonUrl (opens new tab). Items without amazonUrl show price only.
- **"View All on Amazon" button:** Opens all amazonUrl links in new tabs. Gold button variant. Only visible if at least one item has an amazonUrl.
- **Empty state:** When furnitureVisible is false, show "Furniture hidden — toggle to view items" muted text.
- **Responsive:** On desktop, renders as a right sidebar (~320px). On mobile (<md), renders as a collapsible bottom panel.
- Style using project design tokens (Surface bg, Elevated cards, Gold accents).
  </action>
  <verify>
    <automated>cd /Users/padidamabhinay/Desktop/UI/Spazeo && npx tsc --noEmit src/app/tour/[slug]/furnished/[id]/page.tsx src/components/furnished/FurnishedRoomViewer.tsx src/components/furnished/CostSummaryPanel.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Public share page at /tour/[slug]/furnished/[id] renders the furnished room read-only with all navigation modes (orbit/zoom), itemized cost summary with Amazon links, furniture show/hide toggle, and tour branding. No login required.</done>
</task>

</tasks>

<verification>
- TypeScript compilation of all new and modified files passes
- Share page route exists at src/app/tour/[slug]/furnished/[id]/page.tsx
- FurnishedRoomViewer uses public query (no auth check) to load room data
- CostSummaryPanel displays itemized costs with Amazon links
- Furniture toggle shows/hides all placed items in the 3D view
- Save flow creates furnished room and persists placements to Convex
</verification>

<success_criteria>
- Save button persists furniture arrangements to Convex with a generated slug
- Share URL is generated and copyable to clipboard
- Public share page loads without authentication (SHARE-02)
- 3D room renders with all placed furniture in read-only mode
- Cost summary shows subtotal, itemized list, and Amazon product links (SHARE-03)
- Furniture toggle allows comparing empty vs furnished room
- Tour branding is inherited on share page
</success_criteria>

<output>
After completion, create `.planning/phases/03-furniture-catalog-placement-and-room-sharing/03-05-SUMMARY.md`
</output>
