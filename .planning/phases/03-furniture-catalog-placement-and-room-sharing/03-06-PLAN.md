---
phase: 03-furniture-catalog-placement-and-room-sharing
plan: 06
type: execute
wave: 5
depends_on: [03-05]
files_modified:
  - convex/furnitureItems.ts
  - src/app/tour/[slug]/edit/page.tsx
autonomous: false
requirements: [FURN-01, FURN-02]

must_haves:
  truths:
    - "Furniture catalog contains 50+ items across 6 categories with metadata"
    - "Each item has dimensional data (W x D x H in meters), price in USD, and style tag"
    - "Amazon product links are populated where available"
    - "Tour editor page passes enableFurniture=true to GaussianSplatViewer for splat tours"
  artifacts:
    - path: "convex/furnitureItems.ts"
      provides: "Seed script with 52+ items across all categories"
      contains: "seed"
  key_links:
    - from: "src/app/tour/[slug]/edit/page.tsx"
      to: "src/components/viewer/GaussianSplatViewer.tsx"
      via: "enableFurniture={true} prop"
      pattern: "enableFurniture"
---

<objective>
Seed the furniture catalog with 52+ GLB items across 6 categories, wire the tour editor to enable furniture placement for splat tours, and verify the complete end-to-end flow with a human checkpoint.

Purpose: The catalog needs real items for the feature to be usable. The editor page needs to opt-in to furniture features. The final checkpoint validates the entire Phase 3 flow works end-to-end.
Output: Populated catalog, wired editor, and human-verified complete flow.
</objective>

<execution_context>
@/Users/padidamabhinay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/padidamabhinay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-furniture-catalog-placement-and-room-sharing/03-CONTEXT.md
@.planning/phases/03-furniture-catalog-placement-and-room-sharing/03-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create catalog seed data, test GLB primitives, and wire tour editor</name>
  <files>convex/furnitureItems.ts, src/app/tour/[slug]/edit/page.tsx, public/test-glbs/cube.glb, public/test-glbs/cylinder.glb, public/test-glbs/sphere.glb</files>
  <action>
1. **Test GLB primitives:** Create 3 small placeholder GLB files in public/test-glbs/ using a script or by generating minimal valid GLB binaries. Each should be a simple primitive shape (cube, cylinder, sphere) under 10KB. These serve as stand-in models so the checkpoint tester can verify the full placement flow (ghost preview, click-to-place, transform controls) without needing real furniture models. Use Three.js BufferGeometry exports or download from a public GLB test repository (e.g., Khronos glTF-Sample-Models). Alternatively, create a small Node script (scripts/generate-test-glbs.ts) that uses three.js to export basic shapes.

2. **Seed data:** Add a `seedCatalog` mutation to convex/furnitureItems.ts (or enhance the existing `seed` mutation). This creates 52 furniture items distributed across categories:
   - Sofas: 8 items (3-seater sofa, loveseat, sectional L-shape, chaise lounge, futon, recliner, settee, daybed)
   - Beds: 8 items (king bed, queen bed, single bed, bunk bed, platform bed, canopy bed, murphy bed, toddler bed)
   - Tables: 10 items (dining table 6-seat, dining table 4-seat, coffee table, side table, console table, desk, bar table, nightstand, TV console, kitchen island)
   - Chairs: 10 items (dining chair, office chair, accent chair, bar stool, rocking chair, lounge chair, folding chair, bean bag, bench, ottoman)
   - Storage: 8 items (bookshelf, dresser, wardrobe, TV unit, filing cabinet, shoe rack, floating shelf, storage ottoman)
   - Decor: 8 items (floor lamp, table lamp, area rug, indoor plant, wall mirror, picture frame set, vase, throw pillow set)

   Each item includes:
   - name: descriptive English name
   - category: one of the 6 categories
   - style: one of scandinavian/modern/luxury/industrial (distribute evenly — ~13 per style)
   - glbStorageId: Make glbStorageId optional in schema (v.optional(v.id('_storage'))). Seed items with all metadata but no GLB. Add a `setGlbStorageId` mutation that updates a specific item's glbStorageId after file upload. The catalog UI shows items without GLBs as "Coming soon" or with a placeholder icon. This allows the catalog to be browsable immediately.
   - dimensions: realistic dimensions in meters (e.g., 3-seater sofa: { width: 2.2, depth: 0.9, height: 0.85 })
   - priceUsd: realistic USD prices
   - amazonUrl: set for ~60% of items using generic Amazon furniture search URLs (e.g., `https://www.amazon.com/s?k=modern+3+seater+sofa&tag=spazeo-20`). Leave undefined for others. The `spazeo-20` affiliate tag is a placeholder.
   - tags: 2-3 descriptive tags per item (e.g., ['living room', 'seating', 'family'])

3. **Upload helper script:** Add a `uploadTestGlbs` action to convex/furnitureItems.ts that:
   - Fetches the 3 test GLB files from public/test-glbs/ URLs
   - Uploads each to Convex storage via the 3-step pattern
   - Calls setGlbStorageId to assign the storageIds to the first 3 seeded items
   - This gives the checkpoint tester at least 3 items with actual placeable 3D models

4. **Wire tour editor:** Modify src/app/tour/[slug]/edit/page.tsx:
   - When rendering GaussianSplatViewer for a tour that has `splatStorageId` (i.e., it's a 3D splat tour), pass `enableFurniture={true}` and `tourId={tour._id}`.
   - For panorama-only tours (no splatStorageId), do NOT pass enableFurniture (furniture placement only works in 3D splat rooms, not 2D panoramas).

5. **Schema update if needed:** If glbStorageId is currently required in schema.ts, change it to `v.optional(v.id('_storage'))` to allow seeding without actual files. Update the catalog queries to handle items without glbStorageId (filter them out of placement-eligible items, but show them in catalog as "preview only").
  </action>
  <verify>
    <automated>cd /Users/padidamabhinay/Desktop/UI/Spazeo && npx convex dev --once 2>&1 | tail -20</automated>
  </verify>
  <done>Catalog seed mutation exists with 52 items across 6 categories. Test GLB primitives exist for placement verification. Upload helper action can assign test GLBs to seed items. Tour editor passes enableFurniture to GaussianSplatViewer for splat tours. Schema supports optional glbStorageId for graceful seeding without files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete furniture placement end-to-end flow</name>
  <action>Human verifies the complete Phase 3 furniture placement system works end-to-end.</action>
  <verify>Human visual and functional verification of all Phase 3 requirements.</verify>
  <done>All Phase 3 features verified: catalog browsing, search/filter, click-to-place, transform controls, undo, cost tracker with camera centering, save, share link, and public share page.</done>
  <what-built>Complete furniture placement system: catalog sidebar with search/filter, click-to-place in 3D room, transform controls (rotate/scale), undo (Ctrl+Z), delete (Delete key), real-time cost tracker with click-to-center, save/share flow, and public share page with itemized cost summary.</what-built>
  <how-to-verify>
    **PREREQUISITE: Upload test GLBs before testing placement.**
    1. Start dev servers: `npx convex dev` and `npm run dev`
    2. Seed the catalog: Run the seedCatalog mutation from Convex dashboard
    3. Upload test GLBs: Run the uploadTestGlbs action from Convex dashboard (this assigns 3 test GLB primitives to the first 3 seeded items, making them placeable). Alternatively, manually upload 2-3 small GLB files via the Convex dashboard and call setGlbStorageId for specific items.
    4. Verify at least 2-3 catalog items now show as placeable (not "Coming soon")
    5. Open a tour with a 3D splat in the editor (e.g., /tours/[id]/edit)
    6. Click "Furnish" in the mode switcher — catalog sidebar should appear
    7. Search for an item by name — filtered results should show
    8. Click a category tab (e.g., Tables) — grid filters
    9. Click a style filter (e.g., Modern) — grid filters further
    10. Click a placeable item (one with a GLB) — ghost preview should appear following cursor
    11. Click the floor — item places at that position
    12. Click the placed item — Gold outline + transform gizmo appears
    13. Press R — rotate mode. Press S — scale mode. Use gizmo handles.
    14. Press Delete — item removed
    15. Place a few items, check cost tracker updates in sidebar footer
    16. Expand cost tracker, click an item name — verify it selects in 3D AND camera smoothly centers on it
    17. Press Ctrl/Cmd+Z — last action undoes
    18. Click "Save Arrangement" — enter title, save succeeds
    19. Share link is copied — open in incognito window
    20. Verify: furnished room loads read-only, cost summary shows, Amazon links work, furniture toggle hides/shows items
    21. On mobile: catalog appears as bottom sheet, swipeable
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Convex seed mutation deploys without errors
- Catalog contains 52+ items queryable by category, style, and search
- At least 3 items have uploadable test GLBs for placement verification
- Tour editor enables furniture features for splat tours
- End-to-end flow verified by human tester
</verification>

<success_criteria>
- 52+ furniture items seeded with metadata (FURN-01, FURN-02)
- Test GLBs available for verifying placement, ghost preview, and transform controls
- Full placement flow works: browse, place, transform, delete, undo, save, share
- Cost tracker click-to-select centers camera on item
- Share page accessible without login (SHARE-02)
- Cost summary with Amazon links on share page (SHARE-03)
- Mobile bottom sheet catalog works on touch devices
</success_criteria>

<output>
After completion, create `.planning/phases/03-furniture-catalog-placement-and-room-sharing/03-06-SUMMARY.md`
</output>
