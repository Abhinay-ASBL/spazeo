---
phase: 03-furniture-catalog-placement-and-room-sharing
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/furniture/CatalogSidebar.tsx
  - src/components/furniture/CatalogItemCard.tsx
  - src/components/furniture/CostTracker.tsx
  - src/components/furniture/CatalogBottomSheet.tsx
autonomous: true
requirements: [FURN-03, FURN-07]

must_haves:
  truths:
    - "User can search furniture catalog by name and see filtered results"
    - "User can filter catalog by category tabs and style dropdown"
    - "Cost tracker shows running subtotal and item count, expandable to itemized list with Amazon links"
    - "Clicking an item in the cost list selects it in the 3D scene and centers the camera on it"
    - "Mobile users see catalog as a bottom sheet instead of a sidebar"
  artifacts:
    - path: "src/components/furniture/CatalogSidebar.tsx"
      provides: "Right sidebar with search, category tabs, style filter, 2-column item grid"
      min_lines: 80
    - path: "src/components/furniture/CatalogItemCard.tsx"
      provides: "Individual item card with thumbnail, price, dimensions, style badge, Amazon link"
      min_lines: 30
    - path: "src/components/furniture/CostTracker.tsx"
      provides: "Sticky footer with subtotal, item count, expandable itemized list, click-to-select-and-center"
      min_lines: 50
    - path: "src/components/furniture/CatalogBottomSheet.tsx"
      provides: "Mobile bottom sheet variant using framer-motion drag"
      min_lines: 40
  key_links:
    - from: "src/components/furniture/CatalogSidebar.tsx"
      to: "convex/furnitureItems.ts"
      via: "useQuery(api.furnitureItems.list) and useQuery(api.furnitureItems.search)"
      pattern: "useQuery.*furnitureItems"
    - from: "src/components/furniture/CostTracker.tsx"
      to: "src/hooks/useFurnitureStore.ts"
      via: "useFurnitureStore for placedItems, totalCost, setSelectedId, and setCenterOnItem"
      pattern: "useFurnitureStore"
    - from: "src/components/furniture/CatalogItemCard.tsx"
      to: "src/hooks/useFurnitureStore.ts"
      via: "setGhostItem on click (with metadata)"
      pattern: "setGhostItem"
---

<objective>
Build the furniture catalog sidebar UI with search, category tabs, style filter, 2-column item grid, cost tracker footer, and mobile bottom sheet variant. This is the DOM-layer interface users interact with to browse and select furniture items for placement.

Purpose: Users need a browsable, searchable, filterable catalog to find furniture items. The cost tracker gives real-time financial feedback. Mobile bottom sheet ensures touch-friendly experience.
Output: Four new components in src/components/furniture/ ready to be composed into the viewer layout.
</objective>

<execution_context>
@/Users/padidamabhinay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/padidamabhinay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-furniture-catalog-placement-and-room-sharing/03-CONTEXT.md
@.planning/phases/03-furniture-catalog-placement-and-room-sharing/03-RESEARCH.md
@.planning/phases/03-furniture-catalog-placement-and-room-sharing/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — Convex queries to consume -->
From convex/furnitureItems.ts:
```typescript
export const list = query({ args: { category?: string, style?: string }, ... })
export const search = query({ args: { searchTerm: string, category?: string, style?: string }, ... })
// Returns items with resolved glbUrl and thumbnailUrl
```

<!-- From Plan 01 — Zustand store to consume -->
From src/hooks/useFurnitureStore.ts:
```typescript
export type InteractionMode = 'navigate' | 'furnish'
export interface GhostMetadata {
  name: string
  price: number
  amazonUrl?: string
}
export interface PlacedItem {
  instanceId: string
  furnitureItemId: string
  position: [number, number, number]
  rotation: [number, number, number]
  scale: [number, number, number]
  name: string
  price: number
  amazonUrl?: string
  glbUrl: string
}
export const useFurnitureStore = create<FurnitureState>(...)
// Key selectors: mode, placedItems, totalCost, setGhostItem(id, url, metadata),
//   setSelectedId, setCenterOnItem
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CatalogSidebar and CatalogItemCard components</name>
  <files>src/components/furniture/CatalogSidebar.tsx, src/components/furniture/CatalogItemCard.tsx</files>
  <action>
Create src/components/furniture/CatalogSidebar.tsx ('use client'):
- Right sidebar, ~320px wide, fixed height, scrollable content area
- Collapsible via a toggle button (ChevronRight/ChevronLeft from lucide-react). Use framer-motion AnimatePresence for slide-in/out
- **Search bar** at top: text input with Search icon, debounced 300ms. When searchTerm is non-empty, use `useQuery(api.furnitureItems.search, { searchTerm, category, style })`. When empty, use `useQuery(api.furnitureItems.list, { category, style })`.
- **Category tabs** below search: horizontal scrollable row of pill buttons for ['All', 'Sofas', 'Beds', 'Tables', 'Chairs', 'Storage', 'Decor']. 'All' passes undefined for category filter. Active tab uses Gold (#D4A017) background.
- **Style filter** dropdown below tabs: select element with options ['All Styles', 'Scandinavian', 'Modern', 'Luxury', 'Industrial']. 'All Styles' passes undefined.
- **Item grid**: 2-column CSS grid of CatalogItemCard components. Shows loading skeleton while query is pending. Shows "No items found" empty state.
- Clicking an item card calls `useFurnitureStore.getState().setGhostItem(item._id, item.glbUrl, { name: item.name, price: item.priceUsd, amazonUrl: item.amazonUrl })` — passes full metadata so FurnitureLayer can create PlacedItem without a separate lookup.
- Style using Tailwind and project design tokens (Surface bg #12100E, Elevated #1B1916 for cards, text colors from palette).

Create src/components/furniture/CatalogItemCard.tsx ('use client'):
- Displays: thumbnail image (from thumbnailUrl, or a placeholder gradient if null), name, price formatted with Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD' }), dimensions as "W x D x H m", style tag as a small badge, and Amazon link icon (ExternalLink from lucide-react) if amazonUrl exists.
- onClick prop triggers item selection for placement.
- onMouseEnter calls `useGLTF.preload(glbUrl)` to start loading the model before the user clicks (hover-preload strategy per research recommendation).
- Card has 8px border radius, Elevated background, hover state with subtle Gold border.
  </action>
  <verify>
    <automated>cd /Users/padidamabhinay/Desktop/UI/Spazeo && npx tsc --noEmit src/components/furniture/CatalogSidebar.tsx src/components/furniture/CatalogItemCard.tsx 2>&1 | head -20</automated>
  </verify>
  <done>CatalogSidebar renders with search bar, category tabs, style dropdown, and 2-column item grid. CatalogItemCard shows thumbnail, price, dimensions, style badge, and Amazon link. Clicking an item sets the ghost with metadata in the furniture store. Hover-preload triggers useGLTF.preload.</done>
</task>

<task type="auto">
  <name>Task 2: Create CostTracker and CatalogBottomSheet components</name>
  <files>src/components/furniture/CostTracker.tsx, src/components/furniture/CatalogBottomSheet.tsx</files>
  <action>
Create src/components/furniture/CostTracker.tsx ('use client'):
- Sticky footer section within the catalog sidebar (bottom of sidebar, always visible)
- **Collapsed state** (default): shows running subtotal formatted as currency and item count (e.g., "$2,450 (3 items)"). Has a chevron-up button to expand.
- **Expanded state**: scrollable itemized list. Each row shows: item name, price, and a small ExternalLink icon linking to amazonUrl (opens new tab). **Clicking an item row calls BOTH `setSelectedId(instanceId)` AND `setCenterOnItem(item.position)` from useFurnitureStore** — this selects the item in the 3D scene with Gold outline AND animates the camera to center on it (per user decision: "Clicking an item in the cost list selects it in the 3D scene (Gold outline) and centers camera on it").
- **"View All on Amazon"** button at the bottom of the expanded list — opens all amazonUrl links (those that exist) in new tabs. Use Gold button variant.
- Toggle expand/collapse with framer-motion height animation.
- Read placedItems, totalCost, setSelectedId, and setCenterOnItem from useFurnitureStore.
- If no items placed, show "No furniture placed yet" muted text.

Create src/components/furniture/CatalogBottomSheet.tsx ('use client'):
- Mobile variant of CatalogSidebar, rendered instead of sidebar on screens < 768px (use media query or Tailwind md: breakpoint detection).
- Uses framer-motion `motion.div` with `drag="y"` and `dragConstraints` for swipeable behavior.
- Three states: hidden (below viewport), half-screen (50vh, default when furnish mode active), full-screen (drag up to ~90vh).
- Contains the same CatalogSidebar content (search, tabs, filter, grid) composed inside.
- Has a drag handle bar at the top (small rounded pill indicator).
- CostTracker renders at the bottom of the sheet.
- Uses safe-area-inset-bottom padding for notch devices.
  </action>
  <verify>
    <automated>cd /Users/padidamabhinay/Desktop/UI/Spazeo && npx tsc --noEmit src/components/furniture/CostTracker.tsx src/components/furniture/CatalogBottomSheet.tsx 2>&1 | head -20</automated>
  </verify>
  <done>CostTracker shows running subtotal with expandable itemized list and Amazon links. Clicking a cost list item selects it AND centers the camera on it in the 3D scene. CatalogBottomSheet provides a swipeable mobile experience with half/full-screen states. Both read from useFurnitureStore for reactive updates.</done>
</task>

</tasks>

<verification>
- TypeScript compilation of all four components passes without errors
- CatalogSidebar consumes Convex queries (list and search) correctly
- CostTracker derives cost from Zustand store (not Convex query — instant local updates)
- CostTracker click handler calls both setSelectedId and setCenterOnItem
- CatalogBottomSheet uses framer-motion drag for swipe gesture
- All components use project design tokens (Gold, Surface, Elevated colors)
</verification>

<success_criteria>
- Catalog sidebar shows furniture items in a searchable, filterable 2-column grid
- Category tabs filter by category, style dropdown filters by style
- Search debounces input and uses Convex search index
- Cost tracker displays running subtotal and expands to itemized list with Amazon links
- Clicking a cost list item selects it in 3D scene AND centers camera on it
- "View All on Amazon" button opens product links
- Mobile bottom sheet replaces sidebar with swipeable half/full-screen sheet
- Clicking a catalog item sets ghost preview with metadata in store and switches to furnish mode
</success_criteria>

<output>
After completion, create `.planning/phases/03-furniture-catalog-placement-and-room-sharing/03-02-SUMMARY.md`
</output>
