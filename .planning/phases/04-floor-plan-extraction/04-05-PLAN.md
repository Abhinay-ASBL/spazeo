---
phase: 04-floor-plan-extraction
plan: 05
type: execute
wave: 3
depends_on: ["04-03", "04-04"]
files_modified:
  - src/app/(dashboard)/floor-plans/[id]/edit/page.tsx
  - src/components/floor-plan/FloorPlanEditor.tsx
  - src/components/floor-plan/VersionHistory.tsx
  - src/app/(dashboard)/floor-plans/page.tsx
  - middleware.ts
autonomous: false
requirements: [FP-03, FP-04]

must_haves:
  truths:
    - "User can save corrected geometry and it persists across page reloads"
    - "Every save creates a new version in floorPlanVersions"
    - "User can view version history and revert to any previous version"
    - "User can 'Reset to AI result' to restore the original extraction"
    - "Corrected geometry is stored as structured wall coordinates and room metadata in Convex"
    - "Full end-to-end flow works: upload -> extract -> animate -> edit -> save"
  artifacts:
    - path: "src/components/floor-plan/VersionHistory.tsx"
      provides: "Version history panel with restore capability"
      min_lines: 40
    - path: "src/app/(dashboard)/floor-plans/[id]/edit/page.tsx"
      provides: "Complete editor page wiring all components together"
      min_lines: 60
  key_links:
    - from: "src/components/floor-plan/FloorPlanEditor.tsx"
      to: "convex/floorPlans.ts"
      via: "useMutation for updateGeometry (save)"
      pattern: "useMutation.*updateGeometry"
    - from: "src/components/floor-plan/VersionHistory.tsx"
      to: "convex/floorPlanVersions.ts"
      via: "useQuery for version list, restore on click"
      pattern: "useQuery.*floorPlanVersions"
    - from: "src/app/(dashboard)/floor-plans/[id]/edit/page.tsx"
      to: "src/components/floor-plan/FloorPlanEditor.tsx"
      via: "Renders editor after extraction + build-up complete"
      pattern: "FloorPlanEditor"
---

<objective>
Wire the complete end-to-end flow: connect the editor to Convex save/version mutations, add version history UI, update the editor page to transition through all states (extracting -> build-up -> editing), and add a human verification checkpoint.

Purpose: Closes the loop on FP-03 and FP-04 -- corrected geometry persists to Convex with full version history. This is the final plan that makes the entire Phase 4 feature functional end-to-end.
Output: Complete wired editor with save, version history, and end-to-end verification.
</objective>

<execution_context>
@/Users/padidamabhinay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/padidamabhinay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-floor-plan-extraction/04-CONTEXT.md
@.planning/phases/04-floor-plan-extraction/04-RESEARCH.md

@.planning/phases/04-floor-plan-extraction/04-01-SUMMARY.md
@.planning/phases/04-floor-plan-extraction/04-02-SUMMARY.md
@.planning/phases/04-floor-plan-extraction/04-03-SUMMARY.md
@.planning/phases/04-floor-plan-extraction/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire save flow, version history, and complete editor page integration</name>
  <files>src/components/floor-plan/VersionHistory.tsx, src/components/floor-plan/FloorPlanEditor.tsx, src/app/(dashboard)/floor-plans/[id]/edit/page.tsx, src/app/(dashboard)/floor-plans/page.tsx, middleware.ts</files>
  <action>
**VersionHistory.tsx** -- 'use client' component:
- Props: `{ floorPlanId: Id<'floorPlanDetails'>, onRestore: (geometry: FloorPlanGeometry) => void }`
- Uses `useQuery(api.floorPlanVersions.listByFloorPlan, { floorPlanId })` to show version list
- Renders as a collapsible panel (default collapsed) with "Version History" header + ChevronDown toggle
- Each version shows:
  - Version number: "v1", "v2", etc.
  - Source badge: "AI" (teal badge) or "Manual" (gold badge)
  - Timestamp (relative: "2 min ago", "1 hour ago")
  - "Restore" button (secondary variant)
- "Restore" triggers a confirmation dialog: "Restore to version X? Your current changes will be saved as a new version first."
- On restore confirm: save current geometry as new version, then call onRestore with the selected version's geometry
- "Reset to AI Result" as a special button at the bottom that calls the `resetToAiVersion` mutation

**Update FloorPlanEditor.tsx:**
- Add save functionality:
  - "Save" button in toolbar calls `useMutation(api.floorPlans.updateGeometry)` with current store geometry and floorPlanId
  - Auto-save on Ctrl/Cmd+S (preventDefault the browser save dialog)
  - On successful save: set isDirty=false in store, show toast.success("Floor plan saved")
  - Track save in progress state to disable button during save
- Add VersionHistory component to the right sidebar (below PropertiesPanel, or as a separate tab)
- Wire "Reset to AI Result" button to call `useMutation(api.floorPlans.resetToAiVersion)` then update store with restored geometry
- Add unsaved changes warning: beforeunload event listener when isDirty is true
- Ensure imageUrl is fetched: use the floor plan's imageStorageId with Convex storage URL

**Update /floor-plans/[id]/edit/page.tsx:**
- Complete the state machine wiring:
  1. On mount: load project + floor plans via queries
  2. If any floor plan has extractionStatus='pending' or 'processing': show ExtractionProgress
  3. When extraction completes (onComplete callback): transition to 'build-up' state, render AnimatedBuildUp
  4. When animation completes (onAnimationComplete callback): transition to 'editing' state, render FloorPlanEditor with geometry and imageUrl
  5. If floor plan already has extractionStatus='completed' and geometry exists: skip directly to 'editing' state (returning to the editor after initial extraction)
- Multi-floor tab bar: when switching floors, save current floor if dirty (with toast), then load new floor's geometry into store
- Get image URL for each floor plan: use ctx.storage.getUrl pattern or add a query that returns the URL

**Update /floor-plans/page.tsx:**
- Ensure the list page correctly shows project cards with click-through to /floor-plans/[id]/edit
- Add status-specific styling: extracting (amber pulse), editing (gold), completed (green badge)

**Update middleware.ts:**
- Verify /floor-plans/* is protected. If the (dashboard) group layout already handles auth, no changes needed. If not, add '/floor-plans(.*)' to the route matcher.
  </action>
  <verify>
    <automated>cd /Users/padidamabhinay/Desktop/UI/Spazeo && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Save flow persists geometry to Convex via updateGeometry mutation. Version history shows all saves with restore capability. Reset to AI result works. Editor page transitions through extracting -> build-up -> editing states. Auto-save on Ctrl+S. Unsaved changes warning on page leave. Multi-floor tab switching saves before loading new floor.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end floor plan extraction verification</name>
  <files>none</files>
  <action>
Human verifies the complete floor plan extraction flow end-to-end across all four requirements (FP-01 through FP-04).
  </action>
  <verify>Human confirms all 14 verification steps pass</verify>
  <done>All four phase requirements verified working end-to-end</done>
  <what-built>
Complete floor plan extraction feature: upload (PDF/image/sketch), AI extraction via DashScope, animated build-up, interactive 2D editor with drawing tools, save with version history, and dashboard integration.
  </what-built>
  <how-to-verify>
1. Navigate to the dashboard sidebar and verify "Floor Plans" entry appears
2. Click "Floor Plans" -- verify the list page loads (empty state with CTA)
3. Click "Upload your first floor plan" or navigate to /floor-plans/new
4. Test upload: drag a JPG/PNG floor plan image -- verify preview with rotate works
5. Test PDF upload: drag a PDF file -- verify page thumbnails appear, select pages, assign floor numbers
6. Click "Extract" -- verify extraction progress spinner appears
7. After extraction completes: verify animated build-up plays (walls -> rooms -> dimensions)
8. After animation: verify the split-view editor loads:
   - Left: original image with SVG wall overlay
   - Right: editable Konva diagram
9. Test drawing tools: select a wall and drag an endpoint, verify it moves in both views
10. Test properties panel: select a room, edit name and type, verify updates
11. Test zoom/pan: scroll to zoom, drag to pan, verify minimap updates
12. Click "Save" -- reload page, verify geometry persists
13. Check version history: verify "v1 (AI)" and "v2 (Manual)" entries appear
14. Click "Reset to AI Result" -- verify geometry reverts to original extraction
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Full flow: upload -> extract -> animate -> edit -> save works end-to-end
- Geometry persists across page reloads
- Version history shows saves with restore capability
- All four requirements (FP-01 through FP-04) are demonstrable
</verification>

<success_criteria>
- Upload accepts PDF, JPG/PNG, and sketch photos (FP-01)
- AI extracts structured JSON with rooms, walls, dimensions (FP-02)
- Editable 2D diagram with drawing tools and corrections (FP-03)
- Corrected geometry saved to Convex as structured data (FP-04)
- Version history with undo across sessions
- Human verification confirms end-to-end flow
</success_criteria>

<output>
After completion, create `.planning/phases/04-floor-plan-extraction/04-05-SUMMARY.md`
</output>
