---
phase: 04-floor-plan-extraction
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - package.json
  - src/app/(dashboard)/floor-plans/page.tsx
  - src/app/(dashboard)/floor-plans/new/page.tsx
  - src/components/floor-plan/FloorPlanUpload.tsx
  - src/components/floor-plan/FloorPlanPreview.tsx
  - src/components/floor-plan/PdfPageSelector.tsx
  - src/components/layout/Sidebar.tsx
autonomous: true
requirements: [FP-01]

must_haves:
  truths:
    - "User can upload PDF, JPG/PNG, or sketch photo files via drag-and-drop"
    - "Mobile users can capture sketch photos via camera button"
    - "PDF files are rasterized client-side and pages shown as selectable thumbnails"
    - "User can preview uploaded image with rotate (90-degree increments) before extraction"
    - "Multi-floor batch upload lets user assign floor numbers to each file"
    - "50MB per file size limit is enforced"
    - "Floor Plans entry appears in dashboard sidebar"
  artifacts:
    - path: "src/components/floor-plan/FloorPlanUpload.tsx"
      provides: "Drag-and-drop upload with camera capture, multi-file support"
      min_lines: 80
    - path: "src/components/floor-plan/FloorPlanPreview.tsx"
      provides: "Preview with rotate and crop before extraction trigger"
      min_lines: 60
    - path: "src/components/floor-plan/PdfPageSelector.tsx"
      provides: "PDF page thumbnails with floor number assignment"
      min_lines: 50
    - path: "src/app/(dashboard)/floor-plans/new/page.tsx"
      provides: "Standalone upload page at /floor-plans/new"
      min_lines: 30
    - path: "src/app/(dashboard)/floor-plans/page.tsx"
      provides: "Floor plan list/dashboard page"
      min_lines: 20
  key_links:
    - from: "src/components/floor-plan/FloorPlanUpload.tsx"
      to: "react-dropzone"
      via: "useDropzone hook with PDF + image accept types"
      pattern: "useDropzone"
    - from: "src/components/floor-plan/PdfPageSelector.tsx"
      to: "react-pdf"
      via: "Document + Page components for client-side rendering"
      pattern: "react-pdf"
    - from: "src/components/layout/Sidebar.tsx"
      to: "/floor-plans"
      via: "New nav entry in MAIN_NAV array"
      pattern: "floor-plans"
    - from: "src/app/(dashboard)/floor-plans/new/page.tsx"
      to: "convex/floorPlanProjects.ts"
      via: "useMutation for project creation and file upload"
      pattern: "useMutation.*floorPlanProjects"
---

<objective>
Install dependencies, build the floor plan upload experience, and add dashboard navigation. Users can upload PDF/image/sketch files, preview with rotate, assign floor numbers for multi-floor batch uploads, and navigate to the feature from the sidebar.

Purpose: Delivers the complete upload pipeline (FP-01) as a standalone page at /floor-plans/new. Depends on Plan 01 for Convex mutations (floorPlanProjects.create, floorPlanDetails.create, floorPlanActions.extractFloorPlan).
Output: Upload components, PDF rasterization, preview step, floor plan list page, sidebar entry.
</objective>

<execution_context>
@/Users/padidamabhinay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/padidamabhinay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-floor-plan-extraction/04-CONTEXT.md
@.planning/phases/04-floor-plan-extraction/04-RESEARCH.md

@.planning/phases/04-floor-plan-extraction/04-01-SUMMARY.md

@src/components/layout/Sidebar.tsx
@src/components/tour/UploadZone.tsx

<interfaces>
From convex/floorPlanProjects.ts (created in Plan 01):
```typescript
// Mutations
export const create: mutation  // { name, tourId?, floorCount } -> Id<'floorPlanProjects'>
export const update: mutation  // { id, name?, status?, floorCount?, tourId? }
export const remove: mutation  // { id }
// Queries
export const getById: query   // { id } -> FloorPlanProject
export const listByUser: query // {} -> FloorPlanProject[]
```

From convex/floorPlanDetails.ts (created in Plan 01):
```typescript
export const create: mutation  // { projectId?, tourId?, floorNumber, label?, imageStorageId, originalFileType } -> Id<'floorPlanDetails'>
```

From convex/floorPlanActions.ts (created in Plan 01):
```typescript
export const extractFloorPlan: action  // { floorPlanId, imageStorageId }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create upload components</name>
  <files>package.json, src/components/floor-plan/FloorPlanUpload.tsx, src/components/floor-plan/PdfPageSelector.tsx, src/components/floor-plan/FloorPlanPreview.tsx</files>
  <action>
**Step 1: Install dependencies**
```bash
npm install react-konva konva react-pdf use-image
```
Pin pdfjs-dist to v4.x if react-pdf pulls v5+ (check after install, downgrade if needed: `npm install pdfjs-dist@^4.7.0`).

**Step 2: Create FloorPlanUpload.tsx** -- 'use client' component:
- Use react-dropzone with accept: `{ 'application/pdf': ['.pdf'], 'image/*': ['.jpg', '.jpeg', '.png', '.webp'] }`
- 50MB maxSize (50 * 1024 * 1024)
- Multiple files enabled for multi-floor batch upload
- Drag-and-drop zone styled per design system (Carbon Base bg, Gold dashed border on drag-over, Elevated surface)
- Camera capture button for mobile: `<input type="file" accept="image/*" capture="environment">` styled as a secondary button below the dropzone
- File type detection: check MIME type for PDF vs image
- For each dropped file, create a `FloorPlanFile` object: `{ file: File, type: 'pdf' | 'image' | 'sketch', floorNumber: number, label: string, rotation: number, pages?: { pageNum: number, selected: boolean }[] }`
- If PDF detected, render PdfPageSelector for that file
- If image detected, add directly to file list with auto-assigned floor number (sequential starting from 1)
- Show file list with: thumbnail preview, floor number input (number), label input (text), remove button
- "Continue to Preview" button at bottom (disabled until at least one file is ready)

**Step 3: Create PdfPageSelector.tsx** -- 'use client' component:
- Wrap in `dynamic(() => import(...), { ssr: false })` when used -- this component itself uses 'use client'
- Use react-pdf Document + Page to render PDF page thumbnails
- Set worker: `pdfjs.GlobalWorkerOptions.workerSrc = \`https://unpkg.com/pdfjs-dist@\${pdfjs.version}/build/pdf.worker.min.mjs\``
- Show all pages as small thumbnails (150px width) in a horizontal scrollable row
- Each page has a checkbox to select/deselect and a floor number input
- "Select All" / "Deselect All" toggle
- Selected pages will be rasterized to PNG (canvas.toBlob) at 2x scale when user proceeds
- Export a `rasterizePdfPages(file: File, selectedPages: number[], scale?: number): Promise<Blob[]>` utility function

**Step 4: Create FloorPlanPreview.tsx** -- 'use client' component:
- Shows each uploaded file as a large preview card
- Rotate button: cycles through 0, 90, 180, 270 degrees (CSS transform: rotate(Xdeg))
- Floor number and label editable inline
- "Extract Floor Plan" button per file, or "Extract All" button at the top
- Preview card shows image dimensions and file size
- For PDF-sourced images, show "Page X" label
- Pass rotation value to parent so it can be applied before upload

Styling: All components use the dark theme design system. Gold (#D4A017) for primary actions, Elevated (#1B1916) for card backgrounds, proper spacing per 4px grid.
  </action>
  <verify>
    <automated>cd /Users/padidamabhinay/Desktop/UI/Spazeo && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>react-konva, konva, react-pdf, and use-image are installed. Three floor plan upload components exist with drag-and-drop, PDF page selection, camera capture, and preview with rotate. pdfjs-dist is v4.x.</done>
</task>

<task type="auto">
  <name>Task 2: Create floor plan pages and add sidebar navigation</name>
  <files>src/app/(dashboard)/floor-plans/page.tsx, src/app/(dashboard)/floor-plans/new/page.tsx, src/components/layout/Sidebar.tsx</files>
  <action>
**Step 1: Create /floor-plans/new/page.tsx** -- standalone upload page:
- Server component wrapper that dynamically imports the client upload flow
- Page title: "New Floor Plan" with breadcrumb: Floor Plans > New
- Renders FloorPlanUpload as the main content
- After upload + preview, on "Extract" button click:
  1. Upload each image to Convex storage (3-step pattern: generateUploadUrl -> POST -> save storageId)
  2. Apply rotation if non-zero (use canvas to rotate image before upload)
  3. Create a floorPlanProject via `useMutation(api.floorPlanProjects.create)`
  4. Create floorPlanDetails entries for each file via `useMutation(api.floorPlanDetails.create)`
  5. Trigger `useAction(api.floorPlanActions.extractFloorPlan)` for each floor plan
  6. Redirect to /floor-plans/[projectId]/edit (or floor plan list for now since editor comes in Plan 04)
- Show toast notifications for upload progress and extraction trigger

**Step 2: Create /floor-plans/page.tsx** -- floor plan list/dashboard:
- Server component with 'use client' inner content
- Uses `useQuery(api.floorPlanProjects.listByUser)` to fetch projects
- Display as a grid of cards (similar to TourCard pattern):
  - Project name, floor count, status badge (uploading/extracting/editing/completed)
  - Created date
  - Click navigates to /floor-plans/[id]/edit
- Empty state with illustration text: "No floor plans yet" and "Upload your first floor plan" CTA button linking to /floor-plans/new
- "New Floor Plan" button in page header

**Step 3: Add sidebar entry:**
- Import `Map` icon from lucide-react
- Add to MAIN_NAV array after 'Tours' entry:
  ```
  { label: 'Floor Plans', href: '/floor-plans', icon: Map }
  ```

**Step 4: Add route protection:**
- Check middleware.ts -- the (dashboard) layout likely already protects these routes since they're under (dashboard). Verify and add '/floor-plans(.*)' to the route matcher if not covered.
  </action>
  <verify>
    <automated>cd /Users/padidamabhinay/Desktop/UI/Spazeo && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Floor plan pages exist at /floor-plans and /floor-plans/new. Sidebar shows "Floor Plans" navigation entry with Map icon. Upload flow creates Convex storage entries, floor plan projects, and triggers extraction. Route is protected under dashboard layout.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- /floor-plans route renders the list page
- /floor-plans/new route renders the upload page
- Sidebar shows Floor Plans entry between Tours and Analytics
- react-pdf, react-konva, konva, use-image are in package.json dependencies
</verification>

<success_criteria>
- All three file formats accepted: PDF (with page selection), JPG/PNG, sketch photo (camera capture)
- 50MB limit enforced
- Multi-floor batch upload with floor number assignment
- Preview step with rotate (90-degree increments)
- Dashboard sidebar has Floor Plans entry
- Floor plan list page shows projects with status
</success_criteria>

<output>
After completion, create `.planning/phases/04-floor-plan-extraction/04-02-SUMMARY.md`
</output>
